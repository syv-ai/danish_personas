# Copy this file to `.env` and fill in the values. Never commit `.env`.

# Git identity used by `make setup-git` for local commits.
GIT_NAME=
GIT_EMAIL=

# Optional bearer tokens referenced by a local generation configuration.
OPENAI_API_KEY=
HF_TOKEN=
